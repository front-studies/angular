var myVar = 'My Variable';

function myFunction(x, y) {
    return x + y;
}

// ES 6 or ES 2015
let num = 2;
const PI = 3.14;

var numbers = [1, 2, 3];
numbers.map(function(value) {
    return value * 2;
});

numbers.map(value => value * 2);

class MathClass {
    sum(x, y) {
        return x + y;
    }
}

var n1: any = 'sdfsdf';
n1 = 4;
